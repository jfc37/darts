import { GolfScorePipe } from './golf-score.pipe';

describe('GolfScorePipe', () => {
  it('create an instance', () => {
    const pipe = new GolfScorePipe();
    expect(pipe).toBeTruthy();
  });
});
